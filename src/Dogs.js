import React from 'react';
import { Link, useNavigate } from "react-router-dom";

function Dogs() {
    const navigate = useNavigate();
    return (
        <div>
            <h1>Dog info goes here</h1>
        </div>
    );
};

export default Dogs;
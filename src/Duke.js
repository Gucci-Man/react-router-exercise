import React from 'react';
import { Link, useNavigate } from "react-router-dom";

function Duke() {
    const navigate = useNavigate();
    return (
        <div>
            <h1>This is Duke</h1>
        </div>
    );
};

export default Duke;
import React from 'react';
import { Link, useNavigate } from "react-router-dom";

function Whiskey() {
    const navigate = useNavigate();
    return (
        <div>
            <h1>This is Whiskey</h1>
        </div>
    );
};

export default Whiskey;